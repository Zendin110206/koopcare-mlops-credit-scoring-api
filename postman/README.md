# Postman Directory

This directory is reserved for Postman collections or API testing notes.

The collection will be added after the FastAPI endpoints are implemented.

Planned requests:

- `GET /health`
- `GET /model-info`
- `POST /predict`
